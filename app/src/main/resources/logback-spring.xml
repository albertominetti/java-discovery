<configuration>

    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter" />
    <conversionRule conversionWord="wex" converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter" />
    <conversionRule conversionWord="wEx" converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter" />
    <property name="CONSOLE_LOG_PATTERN" value="${CONSOLE_LOG_PATTERN:-%clr(%d{HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>

    <springProperty scope="context" name="graylogHost" source="GRAYLOG_HOST"/>
    <springProperty scope="context" name="graylogPort" source="GRAYLOG_PORT"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

<!--    <appender name="GELF" class="de.siegmar.logbackgelf.GelfTcpAppender">-->
<!--        <graylogHost>${graylogHost}</graylogHost>-->
<!--        <graylogPort>${graylogPort}</graylogPort>-->
<!--    </appender>-->


<!--    <appender name="ASYNC_GELF" class="ch.qos.logback.classic.AsyncAppender">-->
<!--        <appender-ref ref="GELF" />-->
<!--    </appender>-->

    <root level="info">
<!--        <appender-ref ref="ASYNC_GELF" />-->
        <appender-ref ref="STDOUT" />
    </root>

</configuration>